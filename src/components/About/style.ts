import { makeStyles } from "@material-ui/core";

const aboutStyle = makeStyles((theme) => ({
  AboutContainer: {
    padding: '100px'
  },
  Title: {
    fontSize: '46px'
  },
  Content: {
    fontSize: '20px'
  }
}));

export default aboutStyle;